# Instalar as bibliotecas:
# pip install fastapi uvicorn sqlalchemy alembic python-dotenv 
# No alembic rodar python -m alembic revision --autogenerate -m "initial_migration" depois python -m alembic upgrade head 